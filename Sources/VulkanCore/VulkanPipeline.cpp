#include <VulkanImage.h>
#include <VulkanHelper.h>

/*
VulkanImage* VulkanImage::create(VulkanContext& pContext, uint32_t pWidth, uint32_t pHeight);
{
    VulkanImage* lImage = new VulkanImage();

    lImage->mImageExtent = { pWidth, pHeight, 1 };
    lImage->mImageFormat = VK_FORMAT_R8G8B8A8_UNORM;
    lImage->mAllocator = pContext.mDevice;

    vkh::imageCreateInfo();

    lImage->mImage;
    lImage->mImageView;
    
    
    return lImage;
}
*/